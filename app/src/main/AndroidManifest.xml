<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amier.jelajahrasa">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="io.sentry.dsn" android:value="https://fa53051a7d6249458f294119738e0b9c@o422126.ingest.sentry.io/5376677" />

        <activity android:name=".presentation.main.view.DetailActivity">

        </activity>
        <activity android:name=".presentation.main.view.MainActivity">

        </activity>
        <activity android:name=".presentation.ui.auth.view.RegisterActivity" />
        <activity android:name=".presentation.ui.auth.view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>